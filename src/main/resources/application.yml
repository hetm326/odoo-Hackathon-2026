server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/globetrotter?createDatabaseIfNotExist=true
    username: root
    password: Kalp@2018
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

app:
  jwt:
    secret: "CHANGE_THIS_TO_A_LONG_RANDOM_SECRET_KEY_AT_LEAST_32_BYTES"
    expiration-ms: 86400000
  upload-dir: "uploads"
